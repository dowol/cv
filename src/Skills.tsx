import styled from "@emotion/styled";
import {SectionTitle} from "./Components.tsx";

const Section = styled.section`
`;

export default function Skills(){
    return (
        <Section id={'skills'}>
            <SectionTitle>저는 이런 것을 해요</SectionTitle>

        </Section>
    )
}